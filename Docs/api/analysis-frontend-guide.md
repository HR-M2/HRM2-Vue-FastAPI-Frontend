# 综合分析前端集成指南

> **版本**: v2.0  
> **更新日期**: 2025-01-18  
> **适用对象**: 前端开发人员

---

## 一、概述

本文档描述综合分析（最终推荐）模块的完整业务流程，供前端重构参考。

**核心数据流**:
```
沉浸式面试完成 → 创建综合分析 → 展示最终推荐报告
```

**前置条件**:
- 候选人必须有已完成的沉浸式面试会话（`is_completed=true`）
- 沉浸式面试会话必须有 `final_analysis` 数据

---

## 二、完整流程

### 阶段 1: 检查前置条件

在触发综合分析前，需要检查沉浸式面试是否已完成。

**接口**: `GET /api/v1/immersive?application_id={app_id}&is_completed=true`

**前端处理**:
1. 如果返回空列表，提示用户"请先完成沉浸式面试"
2. 如果返回已完成的会话，可以进行综合分析

---

### 阶段 2: 创建/更新综合分析 ⭐

**时机**: 用户点击"生成最终推荐"或"重新分析"按钮

**接口**: `POST /api/v1/analysis`

**请求体**:
```json
{
  "application_id": "app_123"
}
```

**注意事项**:
- 该接口耗时较长（10-30秒），需要显示加载状态
- 重复调用会更新已有分析记录，不会创建新记录
- 建议使用 Loading + 进度提示

**响应处理**:
```javascript
// 示例：调用分析接口
async function createAnalysis(applicationId) {
  try {
    setLoading(true);
    setLoadingText('正在执行 AI 综合分析...');
    
    const response = await api.post('/api/v1/analysis', {
      application_id: applicationId
    });
    
    if (response.success) {
      setAnalysisData(response.data);
      showMessage('综合分析完成');
    }
  } catch (error) {
    if (error.message.includes('LLM服务未配置')) {
      showError('AI 服务未配置，请联系管理员');
    } else {
      showError(error.message);
    }
  } finally {
    setLoading(false);
  }
}
```

---

### 阶段 3: 展示分析结果

综合分析结果包含多个部分，建议分区域展示。

#### 3.1 顶部概览区

展示核心指标：

```
┌─────────────────────────────────────────────────────────┐
│  候选人：张三          应聘岗位：高级软件工程师           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   综合得分        推荐等级          建议行动             │
│   ┌─────┐        ┌─────────┐      ┌───────────────┐    │
│   │78.5 │        │推荐录用 │      │符合要求，可以录用│    │
│   │ 分  │        │         │      │               │    │
│   └─────┘        └─────────┘      └───────────────┘    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**数据字段映射**:
| UI 元素 | 数据字段 |
|--------|---------|
| 候选人 | `candidate_name` |
| 应聘岗位 | `position_title` |
| 综合得分 | `final_score` |
| 推荐等级 | `recommendation_level` |
| 建议行动 | `suggested_action` |

#### 3.2 五维度评估区

展示雷达图 + 详情卡片：

```
┌───────────────────────────────────────────────────────────────┐
│  五维度评估                                                    │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│       [雷达图]                    [维度详情卡片]               │
│                                                               │
│    专业能力 ●────────           ┌────────────────────────┐    │
│           /    \                │ 专业能力 4分 (权重30%)  │    │
│    文化  /      \ 工作          │ ✓ 技术栈匹配度高       │    │
│    匹配 ●        ● 经验         │ ✓ 有大型项目经验       │    │
│          \      /               │ ✗ 算法能力有待提升     │    │
│           \    /                │                        │    │
│    面试    ●──● 软技能          │ 详细分析：候选人具备... │    │
│    表现                         └────────────────────────┘    │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

**数据字段映射**:
```javascript
const dimensionScores = response.data.dimension_scores;

// 遍历展示
Object.entries(dimensionScores).forEach(([key, dim]) => {
  console.log(`维度: ${dim.dimension_name}`);
  console.log(`得分: ${dim.dimension_score}/5`);
  console.log(`权重: ${dim.weight * 100}%`);
  console.log(`优势: ${dim.strengths.join(', ')}`);
  console.log(`不足: ${dim.weaknesses.join(', ')}`);
  console.log(`分析: ${dim.analysis}`);
});
```

**维度 Key 对照**:
| Key | 中文名称 |
|-----|---------|
| `professional_competency` | 专业能力 |
| `work_experience` | 工作经验 |
| `soft_skills` | 软技能 |
| `cultural_fit` | 文化匹配 |
| `interview_performance` | 面试表现 |

#### 3.3 心理分析区

展示三项心理分析结果：

```
┌───────────────────────────────────────────────────────────────┐
│  心理分析                                                      │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 大五人格分析                                             │  │
│  │                                                         │  │
│  │ 性格概括：开放务实、责任心强、善于合作                    │  │
│  │                                                         │  │
│  │ 开放性 ████████░░ 0.75   尽责性 █████████░ 0.82        │  │
│  │ 外向性 ███████░░░ 0.68   宜人性 ███████░░░ 0.71        │  │
│  │ 神经质 ███░░░░░░░ 0.35                                  │  │
│  │                                                         │  │
│  │ 性格优势：做事认真负责、乐于接受新事物                    │  │
│  │ 潜在关注：可能在高压环境下情绪波动                        │  │
│  │ 工作风格：注重计划和条理，善于独立完成任务                │  │
│  │ 团队协作：适合需要协作但也有独立空间的团队                │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 面试可信度                                               │  │
│  │                                                         │  │
│  │ 整体可信度：85% ████████░░ [高可信度]                   │  │
│  │                                                         │  │
│  │ 分析：面试整体可信度为 85.0%，属于高可信度。             │  │
│  │       未发现明显的不可信回答，候选人陈述整体可信。        │  │
│  │                                                         │  │
│  │ [如有低可信度回答，展示列表]                              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 抑郁风险评估                                             │  │
│  │                                                         │  │
│  │ 风险等级：低风险 🟢    平均分数：18.5                    │  │
│  │                                                         │  │
│  │ 解读：候选人在面试过程中整体心理状态良好，               │  │
│  │       未发现明显的抑郁倾向。                              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

**数据字段映射**:
```javascript
const psycho = response.data.input_snapshot.psychological_analysis;

// 大五人格
const bigFive = psycho.big_five;
console.log(`性格概括: ${bigFive.personality_summary}`);
console.log(`开放性: ${bigFive.scores.openness}`);
console.log(`尽责性: ${bigFive.scores.conscientiousness}`);
console.log(`外向性: ${bigFive.scores.extraversion}`);
console.log(`宜人性: ${bigFive.scores.agreeableness}`);
console.log(`神经质: ${bigFive.scores.neuroticism}`);
console.log(`性格优势: ${bigFive.strengths.join(', ')}`);
console.log(`潜在关注: ${bigFive.potential_concerns.join(', ')}`);
console.log(`工作风格: ${bigFive.work_style}`);
console.log(`团队协作: ${bigFive.team_fit}`);
console.log(`详细分析: ${bigFive.detailed_analysis}`);

// 可信度
const credibility = psycho.credibility;
console.log(`可信度: ${(credibility.overall_score * 100).toFixed(1)}%`);
console.log(`等级: ${credibility.level}`);
console.log(`分析: ${credibility.analysis}`);
console.log(`低可信度回答数: ${credibility.low_credibility_responses.length}`);

// 抑郁风险
const depression = psycho.depression;
console.log(`风险等级: ${depression.level_label}`);
console.log(`平均分数: ${depression.overall_score}`);
console.log(`解读: ${depression.interpretation}`);
```

#### 3.4 综合报告区

展示 AI 生成的 Markdown 报告：

```
┌───────────────────────────────────────────────────────────────┐
│  综合分析报告                                                  │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  [Markdown 渲染区域]                                          │
│                                                               │
│  ## 张三 综合分析报告                                         │
│                                                               │
│  **综合评价**：候选人是一位技术扎实、责任心强的软件工程师... │
│                                                               │
│  ### 核心优势                                                 │
│  1. 技术栈与岗位要求高度匹配                                  │
│  2. 有丰富的大型项目经验                                      │
│                                                               │
│  ### 潜在风险                                                 │
│  1. 算法基础有待加强                                          │
│                                                               │
│  ### 心理健康与可信度                                         │
│  候选人面试可信度为 85%，回答真实可信...                      │
│                                                               │
│  ### 最终建议                                                 │
│  建议录用，可安排进入下一轮评估...                            │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

**数据字段**:
```javascript
const report = response.data.report; // Markdown 格式
// 使用 Markdown 渲染库展示
```

---

## 三、完整流程时序图

```
┌─────────┐                                    ┌─────────┐
│  前端   │                                    │  后端   │
└────┬────┘                                    └────┬────┘
     │                                              │
     │  1. 检查沉浸式面试是否完成                    │
     │  GET /api/v1/immersive?application_id=...    │
     │─────────────────────────────────────────────>│
     │<─────────────────────────────────────────────│
     │  返回已完成的会话                             │
     │                                              │
     │  2. 创建综合分析（耗时 10-30秒）              │
     │  POST /api/v1/analysis                       │
     │  {"application_id": "..."}                   │
     │─────────────────────────────────────────────>│
     │                                              │
     │        [显示加载中...]                        │
     │                                              │
     │<─────────────────────────────────────────────│
     │  返回完整分析结果                             │
     │                                              │
     │  3. 展示分析报告                             │
     │  - 顶部概览                                   │
     │  - 五维度评估                                 │
     │  - 心理分析                                   │
     │  - 综合报告                                   │
     │                                              │
```

---

## 四、UI 组件建议

### 4.1 推荐等级徽章

根据 `recommendation_level` 显示不同颜色：

| 推荐等级 | 颜色 | 图标 |
|---------|------|------|
| 强烈推荐 | 绿色 | ⭐⭐⭐ |
| 推荐录用 | 蓝色 | ⭐⭐ |
| 谨慎考虑 | 黄色 | ⚠️ |
| 不推荐 | 红色 | ❌ |

### 4.2 维度评分条

使用进度条展示 1-5 分：

```
专业能力 [████████░░] 4/5 分
```

### 4.3 大五人格雷达图

推荐使用 ECharts 或类似库绘制五维雷达图。

### 4.4 可信度指示器

根据可信度等级显示不同颜色：

| 等级 | 颜色 | 说明 |
|-----|------|------|
| 高可信度 | 绿色 | ≥80% |
| 中等可信度 | 黄色 | 60-80% |
| 低可信度 | 红色 | <60% |

### 4.5 抑郁风险指示器

| 等级 | 颜色 | 图标 |
|-----|------|------|
| 低风险 | 绿色 | 🟢 |
| 中等风险 | 黄色 | 🟡 |
| 高风险 | 红色 | 🔴 |

---

## 五、错误处理

### 常见错误

| 错误信息 | 处理方式 |
|---------|---------|
| LLM服务未配置 | 提示"AI服务未配置，请联系管理员" |
| 应聘申请不存在 | 提示"应聘申请不存在" |
| 网络超时 | 提示"分析超时，请重试"（建议设置 60s 超时） |

### 无沉浸式面试数据

如果候选人没有完成沉浸式面试：
- 分析仍会执行，但心理分析部分为空
- 可在 UI 上提示"未进行沉浸式面试，心理分析不可用"

---

## 六、接口汇总

| 接口 | 方法 | 说明 |
|-----|------|------|
| `/api/v1/analysis` | POST | **创建/更新综合分析（核心）** |
| `/api/v1/analysis` | GET | 获取分析列表 |
| `/api/v1/analysis/{id}` | GET | 获取分析详情 |
| `/api/v1/analysis/{id}` | DELETE | 删除分析 |

---

## 七、完整响应数据结构

```typescript
interface AnalysisResponse {
  id: string;
  application_id: string;
  final_score: number;              // 综合得分 0-100
  recommendation_level: string;     // 推荐等级中文标签
  recommendation_reason: string;    // 推荐理由
  suggested_action: string;         // 建议行动
  
  dimension_scores: {
    [key: string]: {
      dimension_score: number;      // 1-5
      dimension_name: string;
      weight: number;
      sub_scores: { [key: string]: number };
      strengths: string[];
      weaknesses: string[];
      analysis: string;
    }
  };
  
  report: string;                   // Markdown 格式报告
  
  input_snapshot: {
    position: string;
    candidate: string;
    psychological_analysis: {
      big_five: {
        scores: {
          openness: number;
          conscientiousness: number;
          extraversion: number;
          agreeableness: number;
          neuroticism: number;
        };
        personality_summary: string;
        strengths: string[];
        potential_concerns: string[];
        work_style: string;
        team_fit: string;
        detailed_analysis: string;
      };
      credibility: {
        overall_score: number;
        level: string;
        low_credibility_responses: Array<{
          text: string;
          deception_score: number;
          confidence: number;
        }>;
        high_credibility_responses: Array<{...}>;
        analysis: string;
      };
      depression: {
        overall_score: number;
        level: string;
        level_label: string;
        level_distribution: { low: number; medium: number; high: number };
        interpretation: string;
      };
    };
    immersive_session_id: string;
  };
  
  candidate_name: string;
  position_title: string;
  created_at: string;
  updated_at: string;
}
```

---

**如有疑问，请联系后端开发人员。**
